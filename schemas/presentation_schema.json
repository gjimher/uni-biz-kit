{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Presentation Schema",
  "description": "Schema for defining UI presentation settings like locale, currency and number formatting",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Path to the JSON schema"
    },
    "locale": {
      "type": "string",
      "description": "The BCP 47 language tag for the UI locale (e.g., 'es-ES' for Spain, 'en-US' for USA). This determines the default formatting for numbers, dates, and currencies.",
      "default": "en-GB"
    },
    "currency": {
      "type": "string",
      "description": "The ISO 4217 currency code to be used in the application. Examples: 'EUR' for Euro (€), 'USD' for US Dollar ($), 'GBP' for British Pound (£).",
      "default": "EUR"
    },
    "number_formatting": {
      "type": "object",
      "description": "Specific number formatting overrides if the locale defaults are not sufficient.",
      "default": {},
      "properties": {
        "decimal_separator": {
          "type": "string",
          "description": "The character used to separate the integer part from the fractional part (e.g., ',' in Spain, '.' in USA).",
          "maxLength": 1,
          "default": ","
        },
        "thousands_separator": {
          "type": "string",
          "description": "The character used to separate thousands (e.g., '.' in Spain, ',' in USA).",
          "maxLength": 1,
          "default": "."
        }
      },
      "additionalProperties": false
    },
    "menu": {
      "type": "array",
      "description": "Custom left menu structure. If not provided, a flat list of all resources is shown.",
      "items": {
        "$ref": "#/definitions/menuItem"
      }
    }
  },
  "definitions": {
    "menuItem": {
      "type": "object",
      "properties": {
        "label": {
          "type": "string",
          "description": "Display label for the menu item"
        },
        "concept": {
          "type": "string",
          "description": "Name of the concept (resource) to link to. Required for leaf nodes."
        },
        "children": {
          "type": "array",
          "description": "Sub-menu items",
          "items": {
            "$ref": "#/definitions/menuItem"
          }
        }
      },
      "required": ["label"],
      "oneOf": [
        { "required": ["concept"] },
        { "required": ["children"] }
      ],
      "additionalProperties": false
    }
  },
  "required": [],
  "additionalProperties": false
}
