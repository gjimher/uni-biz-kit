{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Presentation Schema",
  "description": "Schema for defining UI presentation settings like locale, currency and number formatting",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Path to the JSON schema"
    },
    "locale": {
      "type": "string",
      "description": "The BCP 47 language tag for the UI messages and defaults (e.g., 'en-GB').",
      "default": "en-GB"
    },
    "number_locale": {
      "type": "string",
      "description": "The BCP 47 language tag specifically for number and currency formatting (e.g., 'de-DE' for 1.234,56 format).",
      "default": "de-DE"
    },
    "currency": {
      "type": "string",
      "description": "The ISO 4217 currency code to be used in the application. Examples: 'EUR' for Euro (€), 'USD' for US Dollar ($), 'GBP' for British Pound (£).",
      "default": "EUR"
    },
    "menu": {
      "type": "array",
      "description": "Custom left menu structure. If not provided, a flat list of all resources is shown.",
      "items": {
        "$ref": "#/definitions/menuItem"
      }
    }
  },
  "definitions": {
    "menuItem": {
      "type": "object",
      "properties": {
        "label": {
          "type": "string",
          "description": "Display label for the menu item"
        },
        "concept": {
          "type": "string",
          "description": "Name of the concept (resource) to link to. Required for leaf nodes."
        },
        "children": {
          "type": "array",
          "description": "Sub-menu items",
          "items": {
            "$ref": "#/definitions/menuItem"
          }
        }
      },
      "required": ["label"],
      "oneOf": [
        { "required": ["concept"] },
        { "required": ["children"] }
      ],
      "additionalProperties": false
    }
  },
  "required": [],
  "additionalProperties": false
}
